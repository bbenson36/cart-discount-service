meta {
  name: Invalid discount code
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/cart/discount
  body: json
  auth: none
}

body:json {
  {
    "items": [
      { "name": "cake", "price": 1000, "quantity": 5 }
    ],
    "discountCodes": ["BOGUS_CODE"]
  }
}

assert {
  res.status: eq 400
  res.body.error: isString
}
